#!/usr/bin/node
// Create and array of string and print them in a loop

const languages = ['C is fun', 'Pyhton is cool', 'Javascript is amazing'];

for (let i = 0; i < languages.length; i++) {
  console.log(languages[i]);
}
